# macOS
.DS_Store

# Local social-media post archive — intentionally untracked project content.
# See social_media_posts/README.md.
social-media-posts/

# Demo/teaching copy of the frontend for YouTube — not deployed, not tracked.
wandern-tests/

# GCS bucket CORS config (local ops artifact)
cors.json

# Jupyter notebooks — local exploration / scratch, not part of the pipeline
*.ipynb
